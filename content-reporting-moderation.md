# 内容举报与审核状态梳理

本文基于当前仓库中的实际实现，梳理举报从提交到审核处置的状态变化。结论只区分两类：

- **业务事实**：写入数据库并决定后续权限、可见性、审核队列是否消失的数据，例如 `report.status`、问题/回答/评论的 `status`、问题的 `show`、内容正文、标签关系、修订记录、活动记录、徽章授予。
- **界面或通知层表现**：根据业务事实派生的待办红点、审核页状态标签、成功提示、搜索结果过滤、站内信/邮件、徽章提示、向量或搜索索引同步。它们失败通常不会回滚已经落库的业务事实。

## 1. 入口和角色

### 1.1 谁能提交举报

提交入口是 `POST /answer/api/v1/report`，由 `ReportController.AddReport` 处理：

- 未登录用户不能提交，控制器直接从登录上下文取 `UserID`。
- 普通用户需要 `report.add` 权限。默认声望门槛是 1；管理员和版主通过角色权限直接拥有该权限。
- 非管理员/版主需要通过举报验证码；管理员和版主跳过验证码。
- 控制器在服务调用后无论成功或失败都会为非管理员/版主记录一次验证码动作，这只影响后续验证码频率，不代表举报已经成立。

服务层 `ReportService.AddReport` 会：

1. 从对象 ID 识别对象类型。
2. 读取对象信息；对象已经删除时拒绝举报。
3. 校验举报理由配置；“重复问题”理由要求补充内容是 URL。
4. 插入 `report` 记录，状态固定为 `pending=1`。
5. 发送 `question.flag`、`answer.flag` 或 `comment.flag` 事件。

当前实际可举报并可被审核处置的是问题、回答、评论。代码注释和前端类型保留了 `user`，但 `ObjService.GetInfo` 与 `ReportHandle.UpdateReportedObject` 都没有用户对象的完整提交流程和处置分支；不应把 UI 类型中的 `user` 当作已实现的用户处罚流程。

### 1.2 谁能审核

审核列表和审核决定都使用同一组公开 API：

- `GET /answer/api/v1/report/unreviewed/post`
- `PUT /answer/api/v1/report/review`

控制器用 `GetUserIsAdminModerator` 判断，角色 ID 为管理员 `2` 或版主 `3` 才允许：

- 非管理员/版主请求列表时返回空页，不查待处理举报。
- 非管理员/版主提交审核决定时返回 403。
- 管理员和版主看到的是同一待处理举报集合，没有按内容类型再做细分授权。

初始化角色权限中，管理员和版主都具备问题、回答、评论的编辑、删除、审核、隐藏/显示等内容权限。不过具体服务仍有少量“只认真正管理员”的判断，评论编辑是最明显的不一致点。

## 2. 核心状态模型

### 2.1 举报单状态

`report.status` 是举报流程的主状态：

| 状态 | 数值 | 何时产生 | 业务含义 |
| --- | --- | --- | --- |
| `pending` | 1 | 举报插入时 | 等待审核，会计入待办和红点 |
| `completed` | 2 | 非忽略操作的内容服务返回成功后 | 审核员已执行编辑、关闭、删除或下架 |
| `ignore` | 3 | 选择忽略后 | 审核员认为无需处理，内容不变 |
| `deleted` | 10 | 仅定义常量 | 当前举报审核流程没有写入该状态 |

审核入口的顺序非常关键：

1. 读取举报单；不存在则报错。
2. 如果举报单不是 `pending`，直接返回成功但不重复执行任何操作。
3. 如果操作是 `ignore_report`，只把举报单改为 `ignore`。
4. 其他操作先调用对应内容服务。
5. 只有内容服务返回 `nil`，才把举报单改为 `completed`。

这里没有数据库事务包住“内容变更”和“举报单状态变更”。因此，内容已经改动但后续某个非关键步骤返回错误时，举报单可能仍停留在 `pending`；反过来，如果内容服务把某些错误只记录日志并返回 `nil`，举报单会完成，即使派生数据没有完全同步。

### 2.2 内容对象状态

问题有两个独立维度：

- `question.status`：`available=1`、`closed=2`、`deleted=10`、`pending=11`。
- `question.show`：`show=1`、`hide=2`。下架只改变 `show`，不改变问题本身是可用还是已关闭。

回答和评论只有主要状态：

- 回答：`available=1`、`deleted=10`、`pending=11`，没有关闭或独立下架字段。
- 评论：`available=1`、`deleted=10`、`pending=11`，没有关闭或独立下架字段。

删除都是软删除，即更新 `status=10`，不是物理删除。评论编辑直接覆盖 `original_text` 和 `parsed_text`，不生成修订；问题和回答编辑会生成修订记录。

## 3. 审核决定到内容状态的映射

`ReportHandle` 按举报对象 ID 分派操作，前端操作和后端状态变化如下。

| 对象 | 审核操作 | 实际服务调用 | 成功后的业务事实 |
| --- | --- | --- | --- |
| 问题 | `unlist_post` | `OperationQuestion(hide)` | `question.show=2`；问题链接隐藏；标签关系改为 hide；记录隐藏活动 |
| 问题 | `delete_post` | `RemoveQuestion(IsAdmin=true)` | `question.status=10`；标签关系删除；问题链接删除；记录删除活动；发送删除事件和向量删除任务 |
| 问题 | `close_post` | `CloseQuestion` | `question.status=2`；写入关闭原因 meta；重复问题可能建立关联；记录关闭活动 |
| 问题 | `edit_post` | `UpdateQuestion(NoNeedReview=true)` | 直接更新标题、正文、标签；生成“已通过”的修订；记录编辑活动；发送更新事件和向量 upsert |
| 回答 | `delete_post` | `RemoveAnswer` | `answer.status=10`；更新问题回答数和用户回答数；记录删除活动；发送删除事件和向量任务 |
| 回答 | `edit_post` | `Answer.Update(NoNeedReview=true)` | 直接更新正文；生成“已通过”的修订；记录编辑活动；可能通知问题作者；发送向量 upsert |
| 评论 | `delete_post` | `Comment.RemoveComment` | `comment.status=10`；发送评论删除事件和父内容向量 upsert |
| 评论 | `edit_post` | `Comment.UpdateComment` | 直接覆盖正文；发送评论更新事件和父内容向量 upsert；不生成修订 |
| 任意 | `ignore_report` | 无内容服务调用 | 仅 `report.status=3` |

前端只给问题展示关闭和下架；编辑和删除适用于问题、回答、评论。这个菜单限制只是 UI 层约束，API 的请求校验只校验五个操作字符串，并不校验“操作是否适用于该对象类型”。

## 4. 部分失败时的真实状态

### 4.1 举报单状态本身

- 举报插入失败：没有举报业务事实，也不会发送举报事件。
- 举报插入成功但事件入队或后续徽章处理失败：举报仍为 `pending`。事件和徽章不是举报提交事务的一部分。
- 内容操作返回错误：举报保持 `pending`，接口返回错误，审核员可以重试。
- 内容操作返回成功但更新举报单为 `completed` 失败：内容已经改变，举报仍是 `pending`。下一次重试会重新读取内容，部分操作可能变成幂等空操作，然后再完成举报。
- 举报已经是 `completed` 或 `ignore` 后重复提交：接口返回成功，但不再执行内容操作。

### 4.2 问题下架

下架不是单纯的界面隐藏，代码会先修改标签关系和问题链接，最后才更新 `question.show=2`：

- 如果问题链接或标签关系处理先成功、最终更新问题失败，数据库中可能已经留下部分关联隐藏，但问题仍显示。
- 置顶问题调用隐藏时，`OperationQuestion` 直接返回 `nil`，不会更新 `show`。由于举报层把 `nil` 当成功，举报会变成 `completed`，但问题仍处于显示状态。这是业务状态与审核结论不一致的实际分支。
- 下架成功后，普通列表 SQL 会要求 `question.show=1`；详情读取也会对非作者、非管理员/版主返回不存在。后台审核页仍可通过不按状态过滤的对象读取接口看到它，并额外显示 unlisted 标签。

### 4.3 问题关闭

关闭流程先写 `question.status=2`，再写关闭原因 meta：

- 状态更新失败：问题和举报都不变。
- 状态更新成功但关闭原因 meta 写入失败：问题已经是 `closed`，举报仍为 `pending`；页面可见性和可回答性按关闭状态生效，但关闭理由数据缺失。
- 重复问题的关联建立没有把错误返回给审核流程；关联失败只影响重复问题关系，不影响举报被标记完成。
- 活动记录通过异步队列写入。活动失败只记录日志，不回滚关闭状态或举报状态。

### 4.4 问题删除

审核删除强制 `IsAdmin=true`，绕过作者、已采纳回答、回答数等普通用户删除限制。删除主状态成功后，后续步骤并不都具备事务一致性：

- 用户问题数更新失败只记录日志，不阻止删除完成。
- 如果关联的机审记录更新失败，服务返回错误；此时问题已经是 `deleted`，举报仍为 `pending`。
- 获取标签失败时记录日志并返回 `nil`，举报会完成；但后续标签关系删除和计数刷新被跳过。
- 标签关系删除、标签计数刷新失败只记录日志，不回滚删除。
- 问题链接删除如果返回错误，服务返回错误；问题已经删除，但举报保持 `pending`。
- 删除活动、删除事件和向量删除都在主状态更新之后异步或尽力发送，失败不影响删除事实。

### 4.5 问题编辑

举报处置中的问题编辑设置 `NoNeedReview=true`，因此不会创建待普通修订审核的记录，而是直接改正文：

- 未删除、无待审核修订、标签和内容长度等校验失败时，正文尚未更新，举报保持 `pending`。
- 正文更新成功但标签变更失败：正文已经改变，举报保持 `pending`。
- 正文和标签成功但修订记录写入失败：页面内容已经改变，举报保持 `pending`，修订历史不完整。
- 修订成功后的活动、事件、向量同步失败不影响正文和举报完成。

该内部分发没有设置 `CanUseReservedTag`。即使管理员/版主通常有保留标签权限，`UpdateQuestion` 仍会按零值 `false` 执行保留标签检查；涉及保留标签的举报编辑可能在正文更新前后因标签校验或变更失败而留下部分更新。

### 4.6 回答删除

回答删除会显式检查操作者角色；管理员和版主绕过作者、票数、采纳等普通用户限制。

- 角色读取失败或普通用户限制不满足时，回答状态不变，举报保持 `pending`。
- `answer.status=10` 写入失败：回答和举报都不变。
- 状态写入成功后，问题回答数、用户回答数、问题链接更新失败都只记录日志，不影响删除和举报完成。
- 删除活动、事件、搜索同步、向量同步都不回滚主状态。
- 回答不存在或已经删除时，服务返回 `nil`，举报会变为 `completed`。这表示“目标已处于删除态/不存在”，不会再额外改变内容。

### 4.7 回答编辑

举报处置设置 `NoNeedReview=true`，因此管理员或版主编辑他人回答时也会直接更新正文，而不是进入修订审核：

- 待修订冲突、回答已删除、父问题不存在、正文相同、链接解析或正文写入失败时，举报按错误路径保持 `pending`；其中“正文相同”返回 `nil`，举报会完成但内容无变化。
- 正文写入成功后，问题更新时间如果失败，会返回错误；回答正文已经改变，举报保持 `pending`。
- 修订记录写入失败时，正文已经改变，举报保持 `pending`。
- 编辑成功后会调用 `notificationUpdateAnswer`。当回答作者不是问题作者时，会给问题作者发送“回答被更新”的站内信；这是回答内容服务的原有副作用，不是专门的举报处置通知。通知入队失败不影响审核完成。

### 4.8 评论删除

评论删除服务本身不再做作者或角色判断，因为公开的评论删除控制器有权限校验；举报分发只由管理员/版主入口进入。

- 评论状态写入失败：评论和举报都不变。
- 状态写入成功后，事件或向量同步失败不影响删除事实。
- 评论不存在或已经因普通查询不可见时，服务返回 `nil`，举报会完成。

### 4.9 评论编辑

评论编辑是当前角色一致性最弱的路径：

- `ReportHandle.updateReportedCommentReport` 构造 `UpdateCommentReq` 时没有设置 `IsAdmin`。
- 评论服务要求：非管理员不能编辑他人评论；非管理员编辑自己的评论也受编辑时限限制。
- 控制器里的“管理员”来自 `GetIsAdminFromContext`，只认角色 ID 2，不包括版主角色 ID 3。

因此，管理员或版主在举报队列中编辑他人评论时，内部分发的 `IsAdmin=false`：

- 如果审核员不是作者，服务返回评论不存在语义的错误，评论不变，举报保持 `pending`。
- 如果审核员恰好是作者且超过编辑时限，也会失败。
- 只有审核员是作者且仍在编辑时限内，才可能通过这条“举报编辑”路径更新成功；这与入口允许管理员/版主处置举报的权限模型不一致。

评论一旦通过校验，正文更新是单表字段覆盖；之后的事件和向量同步失败不影响正文和举报完成。评论不产生修订记录，所以审核编辑后的旧文本不能通过修订表追溯。

## 5. 审核决定与后续展示、通知的一致性

### 5.1 审核队列和红点是派生表现

待处理数量来自 `report.status=pending` 的计数：

- 审核中心的 flagged post 待办数实时统计 pending 举报。
- 通知红点把机审待处理、举报待处理、待审核修订相加。
- 忽略或成功处置后，举报状态不再是 pending，红点和队列下次刷新即消失。
- 如果内容操作成功但举报状态更新失败，红点会继续存在；审核员重试时看到的是最新内容，而不是提交举报那一刻的快照。

审核列表读取对象时使用 `GetUnreviewedRevisionInfo`，故意不按普通前台可见性过滤：

- 问题直接按 ID 读取，包含 `status` 和 `show`。
- 回答直接按 ID 读取。
- 评论使用 `GetCommentWithoutStatus`，所以已删除评论也能出现在待处理举报详情中。

因此审核页中的 available、closed、deleted、pending、unlisted 徽章是对内容字段的展示，不是另一套状态。

审核列表还有一个界面数据错配：数据库中 `report.user_id` 才是举报人，`reported_user_id` 是被举报内容的作者；但组装响应时 `SubmitterUser` 使用了 `report.ReportedUserID`。所以页面“proposed”位置显示的是内容作者，不是举报人。这是展示层取数错误，不改变举报单中的真实业务关系。

### 5.2 普通前台展示

普通内容读取和列表使用数据库状态过滤：

- 问题列表只取可用或已关闭状态，并要求 `show=1`；个人页在授权情况下可包含隐藏内容。
- 问题详情对删除、待审核、隐藏问题限制访问；作者、管理员、版主可见受限对象。
- 回答列表默认只取 `available`；详情对删除、待审核回答以及受限父问题做不存在语义处理，作者和管理员/版主可绕过。
- 评论列表只查询 `CommentStatusAvailable`；单条评论还会通过 `SimpleObjectInfo.CheckVisibility` 检查评论自身及父问题。

因此，删除、关闭、隐藏的权威依据是数据库字段。前端 toast、按钮消失、unlisted 徽章和状态标签都只是这些字段的呈现。

### 5.3 搜索与向量索引

系统里有两套派生索引：

- 旧搜索插件钩子：问题、回答状态或内容更新后，repository 会调用 `UpdateSearch`/`updateSearch`；错误被 `_ =` 忽略。搜索插件拿到的是包含状态的 `SearchContent`，但外部搜索是否立即一致取决于插件实现。
- 向量同步：内容删除、编辑、评论变化后发送内存异步队列任务。队列处理器出错只写日志，没有持久化重试；服务关闭时只等待已接收任务。

这些索引失败不会改变 `question`、`answer`、`comment` 或 `report` 的主状态。普通数据库列表和详情仍以后者为准，但外部搜索结果可能短时间或在任务失败后滞后。

### 5.4 通知和活动

举报提交成功后会发送 flag 事件。该事件唯一明确注册的业务处理是 “FirstFlaggedPost” 徽章规则：

- 徽章授予先写 `badge_award`，再给举报人自己发送成就类通知。
- 徽章或通知失败不回滚举报。
- 被举报内容的作者不会因为“被举报”收到站内信或邮件。
- `object.reported=-100` 配置存在，但当前举报服务没有用它调整作者声望。

审核处置中的通知情况如下：

- 举报队列里的问题删除、问题关闭、回答删除不会发送“你的问题/回答被删除或关闭”的站内信。这类通知存在于另一个管理后台状态修改服务 `AdminSetQuestionStatus`/`AdminSetAnswerStatus`，举报审核没有调用那层通知逻辑。
- 问题和回答删除、关闭、隐藏、编辑会写活动记录，但活动队列是异步的；活动失败只记录日志。
- 回答被编辑时，如果回答作者与问题作者不同，可能给问题作者产生一条“回答被更新”的普通站内信。它来自回答编辑服务，不区分这次编辑是否由举报审核触发。
- 评论删除和评论编辑没有专门的作者通知；评论编辑也没有修订记录。

所以，“用户收到通知”不能证明举报单状态；反过来，举报 `completed` 也不保证一定有面向被举报用户的通知。真正可核对的仍是举报单、内容字段、meta、标签关系、修订和活动表。

## 6. 角色差异和需要特别注意的不一致

1. 审核入口同时接受管理员和版主，列表也按同一待处理集合返回。
2. 问题删除在举报分发中固定 `IsAdmin=true`，因此版主也能绕过普通用户删除约束。
3. 回答删除服务显式允许角色 ID 2 和 3。
4. 评论删除服务依赖入口权限，举报路径上的管理员和版主都可执行。
5. 评论编辑服务只认 `IsAdmin`，而举报分发没有传该字段；版主以及非作者管理员通过举报队列编辑评论会失败。这会造成“可审核、可见编辑按钮、但后端内容服务拒绝”的角色不一致。
6. 问题举报编辑没有传 `CanUseReservedTag`，可能使具备保留标签权限的管理员/版主无法通过举报编辑路径保留或调整保留标签。
7. 置顶问题执行下架时服务返回成功但不改变 `show`，举报会完成而内容仍展示。
8. 不支持的对象和操作组合在分发函数中可能是空操作并返回 `nil`。例如对回答或评论请求关闭/下架，后端没有内容改动，但举报仍会被标记为 `completed`。前端默认不暴露这些组合，但 API 本身没有对象级操作约束。

## 7. 按状态归属的核对清单

### 业务事实

- `report.user_id`：举报人。
- `report.reported_user_id`：被举报对象的作者。
- `report.object_id`、`object_type`：举报对象。
- `report.report_type`、`content`：举报理由和补充内容。
- `report.status`：待处理、已完成、已忽略。
- `question.status`、`question.show`：问题关闭/删除/待审和显示/下架。
- `answer.status`、`comment.status`：回答、评论删除/待审。
- 内容正文字段、标签关系、问题关闭 meta、修订记录。
- 活动记录、徽章授予记录。

### 界面或通知层表现

- 审核中心待办数和通知红点。
- 审核页的状态徽章、unlisted 标签、确认弹窗、成功 toast。
- 前台按钮、权限动作列表、404/不可见效果。
- 站内信、邮件、成就通知。
- 搜索插件索引和向量索引。
- 前端 `SubmitterUser` 的显示值；当前它误取内容作者。

## 8. 主要代码位置

- `internal/controller/report_controller.go`：举报提交、待处理列表、审核决定的登录、验证码和角色入口。
- `internal/service/report/report_service.go`：创建举报、统计待处理、审核状态流转和 flag 事件。
- `internal/entity/report_entity.go`：举报状态常量和表字段。
- `internal/service/report_handle/report_handle.go`：不同内容类型和审核操作到内容服务的映射。
- `internal/service/content/question_service.go`：问题关闭、下架、删除、编辑的状态变化和副作用。
- `internal/service/content/answer_service.go`：回答删除、编辑、可见性和通知副作用。
- `internal/service/comment/comment_service.go`：评论删除、编辑和可见性。
- `internal/service/object_info/object_info.go` 与 `internal/schema/simple_obj_info_schema.go`：审核页读取和普通可见性判断。
- `internal/base/queue/queue.go`：活动、事件、通知、向量任务的异步失败语义。
- `internal/repo/badge/badge_event_rule.go` 与 `internal/service/badge/badge_award_service.go`：首次举报徽章和成就通知。
